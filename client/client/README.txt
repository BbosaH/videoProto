# Crossover Video Portal backend
This is the backend API code that needs to be consumed by front-end applications.

Runing application

start mongoDB 
1) goto directory of installation and start mongd.exe
2) Goto command prompt 
3) navigate to project folder (videoapi)
4) type in command prompt : npm start


The project consist of  a client folder 
Where all the client code exists

for the various unit tests of the client ,continue to naviage to the 'tests folder'